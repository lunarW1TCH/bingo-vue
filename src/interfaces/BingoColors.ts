export default interface BingoColors {
  border: string;
  background: string;
  text: string;
  _id?: string;
}

export const DEFAULT_COLORS = {
  border: '#000000',
  text: '#000000',
  background: '#ffffff',
};
